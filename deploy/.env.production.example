# Copy this file to deploy/.env.production and replace every change-me value.

# Public HTTP entrypoint
HTTP_PORT=80
PUBLIC_BASE_URL=http://your-vps-hostname-or-ip
CORS_ORIGINS=http://your-vps-hostname-or-ip
VITE_API_BASE=

# nginx Basic Auth. Generate this file before starting the stack.
NGINX_HTPASSWD_HOST_PATH=./nginx/.htpasswd
NGINX_BASIC_AUTH_REALM=Hermes Home
APP_UPSTREAM=host.docker.internal:8000

# Host app auth and runtime. bin/hermes-home-install-systemd installs the
# FastAPI app as hermes-home.service and reads this env file.
HOME_API_TOKEN=change-me
HERMES_ENV=production
HERMES_STRICT_DEPLOYMENT_CHECKS=false
LOG_LEVEL=info
CODEX_ENABLED=false
SERVER_PORT=8000
DATABASE_URL=postgresql+psycopg://hermes:change-me@127.0.0.1:5432/hermes_home
OBSIDIAN_VAULT_PATH=/opt/hermes-home/obsidian-vault
AGENT_CMD=/usr/local/bin/hermes run --profile home --input-env HERMES_HOME_COMMAND

# Postgres exposed on localhost for the host systemd app.
POSTGRES_PORT=5432
POSTGRES_DB=hermes_home
POSTGRES_USER=hermes
POSTGRES_PASSWORD=change-me

# Vikunja is exposed on localhost for the host systemd app; nginx does not expose it.
VIKUNJA_PORT=3456
VIKUNJA_IMAGE_TAG=latest
VIKUNJA_SERVICE_PUBLICURL=http://127.0.0.1:3456/
VIKUNJA_SERVICE_SECRET=change-me
VIKUNJA_POSTGRES_DB=vikunja
VIKUNJA_POSTGRES_USER=vikunja
VIKUNJA_POSTGRES_PASSWORD=change-me

# Vikunja todo provider. Create this host file after creating a Vikunja API token.
VIKUNJA_URL=http://127.0.0.1:3456
VIKUNJA_TOKEN=
VIKUNJA_TOKEN_FILE=
VIKUNJA_API_TOKEN_FILE_HOST_PATH=./vikunja/api-token
VIKUNJA_DEFAULT_PROJECT_ID=1
VIKUNJA_PROJECT_ID=
VIKUNJA_TIMEOUT_SECONDS=10
